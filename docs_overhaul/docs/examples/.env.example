## Example environment configuration for the trading bot.
# Copy this file to `.env` and replace the placeholder values.  Never commit
# real secrets to version control.

# -----------------------------------------------------------------------------
# RPC endpoints
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com       # Public RPC for general requests
PRIVATE_SOLANA_RPC_URL=                                  # Optional private RPC used for turbo trades
JITO_RELAY_URL=https://mainnet.block-engine.jito.wtf/api/v1/bundles  # Jito relay for MEV bundles

# -----------------------------------------------------------------------------
# Wallet keys
# Provide the base58 encoded private key for your trading wallet.  This key must
# be encrypted using the envelope scheme documented in docs/WALLET_ENCRYPTION.md.
PRIVATE_KEY=                                             # Required: base58 secret key
PRIVATE_KEY2=                                            # Optional: second wallet for parallel fill

# -----------------------------------------------------------------------------
# Telegram bot
START_TELEGRAM=false                                     # Set to true to enable the Telegram bot
TELEGRAM_BOT_TOKEN=                                      # Bot token from @BotFather

# -----------------------------------------------------------------------------
# Encryption secrets
ENCRYPTION_SECRET=                                       # Required: used to derive KEKs for wallet encryption【732702013707346†L27-L31】
ENCRYPTION_SECRET_OLD=                                   # Optional: previous secret for key rotation

# -----------------------------------------------------------------------------
# Idempotency & caching
IDEMPOTENCY_SALT=my-unique-salt                          # Random string to namespace idempotency keys【732702013707346†L33-L35】
IDEMPOTENCY_TTL_SEC=90                                   # TTL in seconds for idempotency cache【732702013707346†L36-L37】
IDEMPOTENCY_TTL_MS=900000                                # Fallback TTL in milliseconds (e.g. 15min)【732702013707346†L38-L39】

# -----------------------------------------------------------------------------
# Feature flags & strategy control
DISABLED_STRATEGIES=                                     # Comma‑separated list of strategies to disable【732702013707346†L41-L45】
DISABLED_ENDPOINTS=                                      # Comma‑separated list of API endpoints to disable【732702013707346†L41-L45】
START_STRATEGIES=                                        # Specific strategies to auto‑start (comma‑separated)
MAX_CONCURRENCY=4                                        # Max parallel fills across wallets

# -----------------------------------------------------------------------------
# Fees & performance tuning
COMPUTE_UNIT_PRICE=2000                                  # Micro‑lamports per CU; adjust for fee priority【139586722650660†L25-L31】
JITO_TIP_LAMPORTS=0                                      # Lamports tip for Jito bundles【139586722650660†L30-L32】

# -----------------------------------------------------------------------------
# CORS settings
CORS_ENABLED=true                                        # Whether to enable CORS middleware
CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com  # Allowed origins (comma‑separated)
CORS_ALLOW_HEADERS=Authorization,Content-Type            # Allowed headers
CORS_ALLOW_METHODS=GET,POST,PUT,PATCH,DELETE             # Allowed methods

# -----------------------------------------------------------------------------
# Metrics
METRICS_API_KEY=                                         # API key required to access the /metrics endpoint
METRICS_ALLOW_IPS=                                       # Comma‑separated list of CIDRs allowed to scrape metrics

# -----------------------------------------------------------------------------
# Subscriptions & billing
SUBSCRIPTIONS_ENABLED=false                              # Toggle subscription enforcement
STRIPE_WEBHOOK_SECRET=                                   # Secret for Stripe webhook signature verification

# -----------------------------------------------------------------------------
# Database (if using a database)
DATABASE_URL=                                            # PostgreSQL connection string

# -----------------------------------------------------------------------------
# Two‑factor authentication (2FA)
TWO_FA_ENABLED=false                                     # Enable 2FA for API and Telegram
TWO_FA_SECRET=                                           # TOTP secret for the admin user

# -----------------------------------------------------------------------------
# Logging and debugging
DEBUG=                                                   # Comma‑separated list of debug namespaces (e.g. tradeExecutorTurbo)

# -----------------------------------------------------------------------------
# Additional placeholders
# Add any additional environment variables required by custom strategies or services here.
